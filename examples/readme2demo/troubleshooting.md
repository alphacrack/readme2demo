# Troubleshooting

Issues the agent hit while running the README, and how it fixed them.

### adding --break-system-packages flag

**Why:** environment is externally managed and this is a disposable sandbox

Error seen:

```text
$ pip install -e ".[dev]"
error: externally-managed-environment

× This environment is externally managed
╰─> To install Python packages system-wide, try apt install
    python3-xyz, where xyz is the package you are trying to
    install.
    
    If you wish to install a non-Debian-packaged Python package,
    create a virtual environment using python3 -m venv path/to/venv.
    Then use path/to/venv/bin/python and path/to/venv/bin/pip. Make
    sure you have python3-full installed.
    
    If you wish to install a non-Debian packaged Python application,
    it may be easiest to use pipx install xyz, which will manage a
    virtual environment for you. Make sure you have pipx installed.
    
    See /usr/share/doc/python3.13/README.venv for more information.

note: If you believe this is a mistake, please contact 
```

### adding /home/demo/.local/bin to PATH

**Why:** pip installed scripts there but it's not on PATH

Error seen:

```text
$ export PATH="/home/demo/.local/bin:$PATH" && python -m pytest tests/ -q
/bin/bash: line 1: python: command not found
```

### using python3 instead of python

**Why:** python command not found in this environment

Error seen:

```text
$ export PATH="/home/demo/.local/bin:$PATH" && python -m pytest tests/ -q
/bin/bash: line 1: python: command not found
```

*Generated by readme2demo on 2026-07-05.*
